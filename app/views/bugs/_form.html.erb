
Edit this before use

<%= form_for @student, url: url, local: true do |f| %>
	  	 
  <% if @student.errors.any? %>
    <div id="error_explanation">
      <h2>
        <%= pluralize(@student.errors.count, "error") %> prohibited
        this school from being saved:
      </h2>
      <ul>
        <% @student.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <p>
    <%= f.label :name %>
    <%= f.text_field :name %>
  </p>

  <p>
    <%= f.label :std_class %>
    <%= f.text_area :std_class %>    
  </p>

  <p>
    <%= f.label :cgpa %>
    <%= f.text_field :cgpa %>
  </p>

  <p>
    <%= f.label :age %>
    <%= f.text_field :age %>      
  </p>

  <p>
    <%= f.submit %>      
  </p>
<% end %>
